#include "main.h"
/**
 * mul - multiplies two integers
 * @a: character to multiply
 * @b: character to multiply with
 *
 * Return: multiplication result
 */
int mul(int a, int b)
{
	return (a * b);
}

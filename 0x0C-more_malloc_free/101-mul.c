#include "main.h"
#include <stdio.h>
#include <stdlib.h>

#define ERR_MSG "Error"

/**
 * is_digit - checks if a string contains a non-digit character
 * @s: string to be evaluated
 *
 * Return: 0 if a non-digit is found, 1 otherwose
 */
int is_digit(char *s)
{
	int i = 0;

	while (s[i])
	{
}
